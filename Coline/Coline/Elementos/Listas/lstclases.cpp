#include "lstclases.h"

lstClases::lstClases()
{

}

void lstClases::ejecutarMain()
{

    //recorro todas las clases en busca del main

    /*
    for (int i = 0; i < lstClases.count(); ++i) {
        elementoClase *temp=lstClases[i];
        if(temp->lstPrincipal->listaPolimorfa.count()!=0){
            //tiene un metodo principal
            std::cout<<"tiene un metodo principal: "<<temp->nombreClase->valLower.toStdString()<<std::endl;
        }
    }
    */

}
